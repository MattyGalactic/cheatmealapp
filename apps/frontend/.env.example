# Optional for local dev only; defaults to same-origin /api/*
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Server-side Route Handlers (recommended for Vercel)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/cheatmeal
# SEARCH_RADIUS_MILES=10
# PILOT_CITY=Nashville
# PILOT_STATE=TN
