{
  "name": "cheat-meal",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace @cheatmeal/frontend run dev",
    "build": "npm --workspace @cheatmeal/frontend run build",
    "start": "npm --workspace @cheatmeal/frontend run start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
